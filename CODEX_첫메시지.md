# 8/9 Codex 세션 1 — 사용 가이드 (한줄승부)

## 준비 (Codex 실행 전, 5분)
1. 이 폴더(codex-작업폴더)에 SPEC.md, JUDGE_PROMPT.md, data/bosses.json, data/fields.json이
   있는지 확인한다.
2. Codex를 이 폴더에서 실행한다.
3. OpenAI API 키가 아직 없어도 된다 — 세션 1은 MOCK 모드로 완성하는 것이 목표다.
   (키 발급·연동은 세션 1 후반 또는 세션 2에 해도 된다.)

## 첫 메시지 (아래를 그대로 복사해서 붙여넣기)

---

이 폴더에는 "한줄승부"라는 턴제 텍스트 배틀 웹게임의 완성된 설계 문서가 들어 있습니다.
- SPEC.md: 게임 명세 (룰·화면·API 계약·수용 기준)
- IMPLEMENTATION_PLAN.md: 파일별 구현 청사진 (상태 모델·턴 해결 순서·함수 경계·매핑 표)
- JUDGE_PROMPT.md: 판정 서버용 시스템 프롬프트 + 응답 스키마 + 퓨샷
- UI_TEXT.md: 모든 UI 문구 확정본
- data/: 보스·필드·모션 데이터
- libs/: three.js v0.185 + GLTFLoader (이미 벤더링 완료 — 다운로드·npm 불필요, 수정 금지)

다음 순서로 작업해 주세요.

1. 위 문서 5종을 먼저 읽으세요. IMPLEMENTATION_PLAN.md의 상태 모델(1장)과
   턴 해결 순서(2장)는 반드시 그대로 구현해야 합니다.
2. 명세 간 모순이나 구현 전에 반드시 확인이 필요한 점이 있으면 지금 한 번에 모아서
   질문하세요. 사소한 것은 질문하지 말고 문서의 의도에 맞게 스스로 판단하세요.
3. IMPLEMENTATION_PLAN.md 6장의 구현 순서(1~8단계)대로 구현하고, 각 단계가 끝날 때마다
   git 커밋을 남기세요. UI 문구는 UI_TEXT.md의 것을 그대로 사용하세요.
4. 구현이 끝나면 SPEC.md 10장의 수용 기준을 하나씩 자체 점검하고 통과 여부를 표로
   보고하세요. 통과하지 못한 항목은 이유를 적으세요.

주의사항입니다.
- data/*.json과 JUDGE_PROMPT.md의 내용은 수정 금지입니다. 문제를 발견하면 보고만 하세요.
- API 키를 코드나 저장소에 넣지 마세요. .env와 wrangler secret만 사용합니다.
- SPEC.md 11장 "하지 말 것"을 지키고, 명세에 없는 기능을 추가하지 마세요.
- 코드 주석과 README는 한국어로 작성하세요.

---

## 세션 운영 요령 (한도 절약)
- 첫 메시지 후 질문이 오면 한 번에 모두 답하고, 구현이 끝날 때까지 개입하지 않는다.
- 완성 보고가 오면 직접 플레이한다.
  - 터미널 1: `MOCK=1 node server/server.mjs` (윈도우 PowerShell은 `$env:MOCK="1"; node server/server.mjs`)
  - 터미널 2: `npx serve` → 브라우저에서 접속.
- 발견한 문제는 메모했다가 한 번에 보낸다 (증상 + 재현 방법 목록).
- 목표 왕복: 첫 지시 1회 + 질문 응답 1회 + 버그 일괄 수정 1~3회.
- 세션 종료 기준: MOCK 모드로 보스 1~2차전 완주 가능이면 성공. 실제 API 연동은
  코드만 되어 있으면 키 연결은 나중에 해도 된다.
- Codex 화면을 중간중간 스크린샷으로 저장한다 (심사 기준 3번·5번 증빙).

## 세션 1 이후 (평일, Codex 불필요)
- OpenAI API 키 발급 (platform.openai.com, 결제 한도 $10 설정) → server/.env에 넣고
  실제 판정 테스트 → 판정 품질 메모 (재미없는 판정, 이상한 수치 등).
- 판정 품질 문제는 JUDGE_PROMPT.md 수정으로 해결한다 (Claude와 평일에 다듬기 — 코드 수정 불필요).
