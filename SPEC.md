# 한줄승부 (One-Line Duel) — 구현 명세서 v2.0

당신이 쓴 문장이 무기가 된다. 배틀 전에 한 줄로 캐릭터를 만들고, 매 턴 한 줄의 기술을 써서
겨루고, AI 심판이 두 문장의 상호작용을 판정한다. 치명상을 입으면 마지막 발악의 기회가 온다.

이 문서 하나로 게임 전체를 구현할 수 있도록 작성했다. 판정 프롬프트는 JUDGE_PROMPT.md,
보스·필드·모션 데이터는 data/ 폴더에 있다. 명세에 없는 세부는 상식적으로 판단하되,
룰·API 계약·수용 기준은 반드시 이 문서를 따른다.

## 0. 설계 의도 (구현 참고용)

기존 텍스트 배틀류(등록형 원샷 판정)와 달리 본작은 매 턴 새 문장을 쓰는 실시간 공방이다.
차별 요소: ① 캐릭터 고유 속성이 전투 내내 판정에 반영 ② 기력(글자 수=코스트) 자원 관리
③ HP 깎기가 아닌 부상·치명상·발악의 서사적 승부 ④ 필드 카드 상성 ⑤ 판정 결과가
사전 제작 모션으로 렌더링되는 연출 ⑥ 한 기기 2인 대전.

## 1. 게임 모드

1. **도전 모드 (v1 핵심)**: 내 캐릭터를 만들어 보스 5명을 차례로 격파.
2. **대전 모드 (v1 포함, 컷 1순위)**: 한 기기 2인. 각자 캐릭터를 만들고 번갈아 입력.
3. 온라인 래더는 v1 범위 밖. 구현하지 않는다.

## 2. 캐릭터 시스템

- 도전 모드 첫 진입 시 캐릭터를 만든다. **이름(6자 이내) + 고유 속성(100자 이내, 공백 제외)**.
  예시 버튼으로 3개 견본 제공:
  - "민수: 몸이 바람처럼 빨라 공격을 피하고 등 뒤로 파고드는 것이 특기다"
  - "철수: 눈치가 빨라 상대의 행동을 한 발 먼저 예측한다"
  - "봉식: 맷집이 좋아 어지간한 충격은 웃으며 버틴다"
- 고유 속성은 전투 내내 매 턴 판정에 전달된다. 속성과 결이 맞는 기술은 강해지고,
  속성과 모순되는 기술은 약해진다 (판정 프롬프트가 처리).
- **금지 속성 필터 (클라이언트)**: "무적", "모든", "즉사", "절대", "심판" 포함 시 저장 거부하고
  안내 문구 표시. 우회 시도는 판정의 반칙 규정이 잡는다.
- 캐릭터는 localStorage에 저장, 언제든 재작성 가능. 보스는 data/bosses.json의 trait을 쓴다.

## 3. 배틀 룰

### 3.1 기력 (자원)
- 시작 60, 매 턴 시작 시 +40, 최대 100. **기술 코스트 = 공백 제외 글자 수.** 평상시 최대 60자.
- 입력창 옆에 "23자 / 기력 74" 실시간 표시, 초과 시 전송 비활성.

### 3.2 부상 시스템 (HP 없음)
- 상태 단계: **건재(0) → 경상(1) → 중상(2) → 빈사(3)**. 부상 아이콘(🩸)으로 표시.
- 판정은 매 턴 각자에게 부상 증가 0/1/2를 준다. 2는 치명타(정통 직격·완벽한 카운터)일 때만.
- 회복 판정(recover 1)은 부상 1단계를 되돌린다. 배틀당 최대 2회까지만 인정 (클라이언트 강제).
- 부상 합계가 3(빈사)에 도달하면 즉시 **발악 규칙** 발동.

### 3.3 발악 (배틀당 1회)
- 빈사가 된 캐릭터는 다음 턴에 마지막 발악을 한다. 그 턴만 **글자 제한 100자, 기력 무제한**.
  화면에 붉은 비네트 + "마지막 발악!" 배너.
- 발악 턴 판정 후: 상대에게 부상 1 이상을 입혔으면 기사회생 — 본인 부상이 중상(2)으로 회복되고
  전투 속행. 입히지 못했으면 그대로 패배.
- 이미 발악을 쓴 캐릭터가 다시 빈사가 되면 즉시 패배. 보스도 같은 규칙 (발악 턴에는
  enraged 기술을 사용).
- 양쪽이 같은 턴에 빈사가 되면 둘 다 발악 턴을 가진다 (동시 판정 1회).

### 3.4 턴 구조 — 도전 모드 (판정 API는 턴당 1회)
1. **필드 공개**: 시작 시와 3턴마다 필드 카드 교체 (data/fields.json, 시드 순서).
2. **보스 예고**: 보스 기술 풀에서 클라이언트가 규칙 기반 선택 (LLM 호출 없음).
   규칙: 부상 2 이상이면 enraged 우선, 같은 기술 2연속 금지, 그 외 랜덤. 예고문(tease)만 표시.
3. **플레이어 입력**: 예고를 보고 한 줄 기술 작성. 제한 시간 없음.
   입력하는 동안 연출 레이어는 슬로우 모션 대치 상태 (5.3).
4. **판정**: 양측 문장 + 고유 속성 + 부상·상태 + 필드를 판정 API로 전송.
5. **연출**: 내레이션 타이핑 출력 + motions·effects 재생 + 부상·상태 적용.
- 12턴 초과 시 부상 적은 쪽 승리, 동수면 무승부 (도전 모드에서는 재도전 처리).

### 3.5 턴 구조 — 대전 모드
P1 입력 → 가림 화면("기기를 넘겨주세요") → P2 입력 → 판정 1회 → 연출. 예고 단계 없음(동시 심리전).

### 3.6 상태 효과 (4종만)
`화상`(다음 턴 자기 기술 위력 약화 — 판정에 전달), `감전`(다음 턴 코스트 +30% — 클라 강제),
`보호막`(다음 부상 1회 무효 — 클라 강제), `혼란`(다음 턴 최대 30자 — 클라 강제). 1턴 지속.

### 3.7 판정 불능 폴백
타임아웃 8초 → 재시도 1회 → 실패 시 약식 판정: 코스트 큰 쪽이 부상 1을 입힘,
내레이션 "심판이 자리를 비웠다… 기세로 승부가 갈렸다!". 게임은 절대 멈추지 않는다.

## 4. 판정 API 계약

클라이언트는 판정 서버의 `POST /judge`만 호출한다. 키는 서버에만 둔다.

요청:
```json
{
  "mode": "boss",
  "field": "그믐밤: 어둠 속성이 강해지고, 빛 속성은 약해진다. 은신이 쉬워진다",
  "turn": 5,
  "p1": { "name": "민수", "trait": "몸이 바람처럼 빨라 공격을 피하고 등 뒤로 파고드는 것이 특기다",
          "wounds": 2, "statuses": ["화상"], "last_stand": false,
          "text": "어둠에 섞여 등 뒤로 돌아가 발목을 벤다", "cost": 17 },
  "p2": { "name": "묵혼", "trait": "상대가 쓴 문장의 단어를 기억해 되돌려 쓸 수 있다. 그림자 속에서는 실체가 없다",
          "wounds": 1, "statuses": [], "last_stand": false,
          "text": "그림자 잉크를 쏟아 전장을 검게 물들인다", "cost": 17,
          "personality": "낮고 음산한 문어체를 쓰는 그림자 사서" }
}
```

응답 (structured output으로 강제, 스키마는 JUDGE_PROMPT.md):
```json
{
  "narration": "잉크가 번지는 어둠은 묵혼의 영토이자 민수의 은신처. 검은 물결 속에서 민수의 칼날이 먼저 발목을 스친다!",
  "p1": { "wound": 0, "recover": 0, "add_statuses": [], "shout": "어둠은 나도 쓸 줄 알아" },
  "p2": { "wound": 1, "recover": 0, "add_statuses": [], "shout": "…제법이군" },
  "effects": [ { "type": "dark", "target": "p2", "intensity": 2 } ],
  "motions": [ { "actor": "p1", "motion": "teleport" }, { "actor": "p1", "motion": "slash" } ],
  "verdict": "p2_hit"
}
```

- wound는 "그 캐릭터가 입는 부상"이다. motions의 motion 값은 data/motions.json의 combat 모션 id만.
- 모델: 환경변수 `OPENAI_MODEL`, 기본 `gpt-5-mini`. max_output_tokens 500.

## 5. 연출 레이어 (three.js 3D 렌더러)

### 5.1 구조
- js/render3d.js가 렌더러 인터페이스를 구현한다. battle.js는 renderer를 주입받는 구조로
  만들어, 비상시 2D 폴백(14장)으로 교체 가능하게 한다.
- 인터페이스: `init(container)`, `setActors(p1Info, p2Info)`, `setPhase("typing"|"resolving"|"idle")`,
  `playTurn(motions, effects, onDone)`, `setWounds(side, n)`, `lastStand(side)`, `finish(winner)`.

### 5.2 three.js 3D 렌더링
- **three.js는 libs/에 이미 벤더링되어 있다** (libs/three.module.js v0.185,
  libs/addons/loaders/GLTFLoader.js). CDN·npm 불필요. index.html에 importmap을 선언한다:
  `{"imports": {"three": "./libs/three.module.js"}}` (GLTFLoader는 상대 경로로 import).
- **캐릭터**: 프리미티브 조립 휴머노이드 2체 — 구(머리) + 캡슐(몸통·팔뚝·허벅지·정강이) +
  관절 Group 계층 (root > torso > shoulderL/R > elbowL/R, root > hipL/R > kneeL/R > 발).
  p1 파랑 계열, p2는 보스 속성색 머티리얼. 그림자 없어도 된다 (성능 우선).
- **GLB 교체 훅**: data/models/p1.glb·p2.glb 파일이 존재하면 GLTFLoader로 로드해 프리미티브
  대신 사용한다 (본이 없는 통짜 메시면 모션은 루트 이동·회전·기울임만 적용). 파일이 없으면
  조용히 프리미티브를 쓴다. 팀원이 외부 도구로 뽑은 3D 모델을 꽂기 위한 훅이다.
- **무대**: 바닥 원판 + 필드에 따른 배경색·포그 변경, 조명은 ambient + directional 각 1개.
- **모션**: data/motions.json의 keyhint를 관절 각도 키프레임으로 해석해 보간 재생
  (모션당 0.5~1.2초, 순차 재생). 이동이 필요한 모션(teleport, grab_throw 등)은 root 위치 보간.
- **카메라 워크**: 기본 3/4 부감 고정 → typing 중 느린 궤도 회전 → 판정 도착 순간 피격자
  쪽으로 줌 펀치 + 흔들림 → 복귀. intensity 3이면 0.3초 슬로우 줌을 먼저 넣는다.
- **이펙트**: 속성색 파티클(THREE.Points 또는 스프라이트 다발), explosion은 확장하는 링 메시.
  intensity 3에서 카메라 셰이크.
- **부상 표현**: 경상 = 평소, 중상 = 상체 15도 숙임, 빈사 = 무릎 꿇기 포즈.

### 5.3 슬로우 모션 대치 (컷 금지)
- phase "typing" 중 두 캐릭터가 0.3배속으로 천천히 원을 그리며 대치하고 이따금 잽·페인트
  동작을 한다. "resolving"에서 배속이 점점 오르다가 판정 도착 순간 본 모션이 터진다.
  카메라 궤도 회전과 함께 이 게임 연출의 핵심이다.

## 6. 화면 구성 (모바일 우선)

1. **홈**: 로고, 내 캐릭터 카드(이름·속성, 수정 버튼), "도전 모드", "2인 대전", "오늘의 도전",
   설정(오프라인 모드, 판정 서버 주소).
2. **캐릭터 작성**: 이름 + 속성 입력(글자 수 표시), 예시 3개 버튼, 금지어 안내.
3. **배틀 화면** (세로 1열):
   - 상단: 상대 이름·칭호·부상 아이콘·상태 아이콘, 필드 카드 칩.
   - 중앙: **연출 canvas (스틱맨 무대, 화면의 35~40%)**.
   - 그 아래: 배틀 로그 (예고·기술·내레이션 말풍선, 내레이션은 타이핑 효과).
   - 하단: 내 부상·기력바, 입력창(60자), 글자수/기력 표시, 전송 버튼.
4. **결과 화면**: 승패, 명장면 다시보기(최고 verdict 턴 내레이션 + 모션 리플레이),
   전투 로그 복사, 다음 보스 / 재도전.

## 7. 판정 서버 (v1.0과 동일 구조)

- server/server.mjs: Node 18+, 의존성 없음, .env(OPENAI_API_KEY, OPENAI_MODEL), 포트 8787,
  CORS localhost, `MOCK=1` 목업 모드.
- 목업 판정 규칙: 코스트 큰 쪽이 부상 1을 입힘(코스트 차 15 이상이면 2), 속성 키워드로
  effects·motions 태그 생성(불→fire+flame, 검·베→slash, 총→shoot 등 단순 매핑),
  내레이션 템플릿 3종. 오프라인 시연·개발용.
- worker/worker.js: 동일 인터페이스, wrangler secret, IP당 분당 6회·일 200회 제한, 요청 검증.

## 8. 데이터 파일 (내용 수정 금지, 문제는 보고만)

- **data/bosses.json**: 보스 5명. hp 대신 **endurance(빈사 도달 부상 수: 짚단2, 벽염3, 로가3, 팽3, 묵혼4)**
  와 **trait(고유 속성)** 포함. skills[].tease가 예고문.
- **data/fields.json**: 필드 12장.
- **data/motions.json**: 모션 라이브러리 (combat 26종 + system 4종). 각 모션의 keyhint대로 구현.

## 9. 저장 (localStorage "old:v1")

캐릭터(이름·속성), 도전 진행(격파 보스, 발악·회복 사용 내역은 배틀 중 상태로만),
오늘의 도전 결과(날짜별), 설정, 전적.

## 10. 파일 구조

```
index.html         (importmap 선언 포함)
css/style.css
js/main.js        js/battle.js      js/judge.js
js/render3d.js    js/boss.js        js/local2p.js
js/character.js   js/storage.js
libs/three.module.js  libs/addons/loaders/GLTFLoader.js   (이미 있음, 수정 금지)
data/bosses.json  data/fields.json  data/motions.json
data/models/      (GLB 훅용, 비어 있어도 됨)
server/server.mjs server/.env.example
worker/worker.js  worker/wrangler.toml
README.md
```

## 11. 기술 제약

- 클라이언트: 순수 HTML/CSS/JS(ES 모듈), 프레임워크·빌드·CDN·외부 폰트 금지.
  three.js는 libs/에 벤더링된 것만 import한다 (네트워크 요청 0 원칙 유지).
- 유일한 네트워크 요청은 판정 서버 /judge. JUDGE_URL은 설정에서 변경 가능.
- GitHub Pages 정적 호스팅 가능 (상대 경로만).

## 12. 수용 기준 (구현 완료 후 자체 점검·보고)

- [ ] 캐릭터 작성(예시 버튼·금지어 필터 포함) 후 MOCK 모드로 보스 1~2차전 완주 (승·패 모두).
- [ ] 고유 속성이 매 턴 판정 요청에 포함된다.
- [ ] 부상 0/1/2 적용, 빈사 시 발악 턴(100자·기력 무제한·배너), 기사회생과 최종 패배가 모두 동작.
- [ ] 회복 2회 제한, 부상 상한, 상태 4종(감전 코스트 증가·혼란 30자·보호막 무효 1회)이 클라에서 강제.
- [ ] 실제 API 키 연결 시 같은 흐름이 실판정으로 동작.
- [ ] 판정 서버가 죽어도 약식 판정으로 진행.
- [ ] 3D 캐릭터 2체가 typing 중 슬로우 대치 + 카메라 궤도 회전, 판정 도착 시 motions 순차
      재생과 카메라 줌 펀치, intensity 3 셰이크가 동작한다.
- [ ] data/models/에 GLB가 없어도 프리미티브 휴머노이드로 정상 동작한다.
- [ ] 모션 26종이 각각 시각적으로 구분된다 (완성도보다 구분 가능성이 기준).
- [ ] 2인 대전: 캐릭터 2개 작성 → 가림 화면 → 동시 판정.
- [ ] 보스 5연전 진행 저장, "오늘의 도전"이 같은 날짜에 같은 순서 (KST).
- [ ] 360px 폭 대응. node server.mjs가 .env만으로 기동, worker가 wrangler 배포 가능 형태.

## 13. 하지 말 것

온라인 매치·계정·DB·결제·애널리틱스. 키를 클라이언트에 두기. data/*.json과 JUDGE_PROMPT.md 수정.
명세에 없는 기능 추가.

## 14. 시간 부족 시 컷 우선순위 (위에서부터)

1. 2인 대전 (local2p.js).
2. "오늘의 도전" 데일리 시드.
3. 모션 26종 → 핵심 12종(slash, stab, shoot, cast, flame, teleport, dodge, block, counter,
   charge_up, explosion, punch_rush)으로 축소, 나머지는 가장 비슷한 모션으로 대체 매핑.
4. 상태 효과 4종 → 보호막 1종.
5. (비상) render3d가 2시간 이상 막히면: 같은 인터페이스의 canvas 2D 스틱맨 렌더러로 전환
   (모션 키프레임 구조는 동일하게 유지).
절대 유지: 캐릭터 고유 속성, 부상·발악 시스템, LLM 판정 + 목업 폴백, 기력 시스템,
슬로우 대치 연출 (3D든 2D든).
